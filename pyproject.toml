[project]
name = "not_that_movie"
version = "1.0.0"
description = "Webapp for generating badly translated movie descriptions."
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4",
    "flask",
    "google-cloud-storage>=2.10.0",
    "googletrans==4.0.2",
    "gunicorn",
    "openai>=2.33.0",
    "pillow>=12.2.0",
]

[dependency-groups]
dev = [
  "ipython>=9.8.0",
  "pytest",
  "pytest-asyncio>=1.3.0",
  "pytest-mock>=3.15.1",
  "pytest-schema",
]
